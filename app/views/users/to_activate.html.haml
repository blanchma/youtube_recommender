%table
  %thead
    %tr
      %th Email
      %th Signed Up
      %th FB?
      %th TW?
      %th
  %tbody
    - @users.each do |user|
      - form_for user, :url => activate_user_path(user), :method => :put do |f|
        %tr
          %td= user.email
          %td= user.created_at.to_s(:short)
          %td= user.facebook?
          %td= user.twitter?
          %td= f.submit "activate!", :class => 'awesome magenta'